[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = ".env"
  
  [build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"